#!/usr/bin/env bash
# Bearcove capn: rustfmt on staged files, Cargo.lock staging, arborium,
# edition-2024 + external-path-deps enforcement. See .config/capn/config.styx.
set -e
capn

# Spec ↔ impl ↔ verify coverage. Architect-specific addition.
if command -v tracey >/dev/null 2>&1; then
    tracey query validate
fi
