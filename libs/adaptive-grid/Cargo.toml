[package]
name = "adaptive-grid"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
description = "A musical grid that follows the zoom — the division that keeps gridlines a readable distance apart, as pure arithmetic"

# No dependencies, deliberately. This is arithmetic over a zoom level and
# a grid division; anything it linked would be carried by every surface
# that wants a grid, and the point of a separate crate is that the
# arrange view and the expression editor can agree without either
# depending on the other.
[dependencies]
