[package]
name = "level-dsp"
version.workspace = true
edition = "2021"          # matches sibling fx DSP crates (comp-dsp, pitch-dsp)
authors.workspace = true
license.workspace = true
description = "level-dsp — vocal leveling engine: block classifier, realtime rider/gate/de-esser/de-breather, and offline analyze→envelope."

[dependencies]
audiocore-dsp = { workspace = true }

[dev-dependencies]
hound = "3.5"
