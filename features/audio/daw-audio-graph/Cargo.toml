[package]
name = "daw-audio-graph"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Core audio processing DAG engine — AudioNode trait, BufferPool, and graph compiler."

[package.metadata]

[package.metadata."docs.rs"]
rustdoc-args = ["--html-in-header", "arborium-header.html"]

[lib]
path = "src/node.rs"

[lints]
workspace = true
