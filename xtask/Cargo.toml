[package]
name = "daw-xtask"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
reaper-launcher = { workspace = true, features = ["icon-gen"] }
serde_json = "1.0"
